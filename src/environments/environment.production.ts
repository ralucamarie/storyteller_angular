// Production environment.
// apiBaseUrl must point to the deployed Django API (https, with /api suffix).
export const environment = {
  production: true,
  apiBaseUrl: 'https://api.povesticupesti.ro/api',
};
