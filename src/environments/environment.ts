// Default (development) environment.
// Replaced at build time by environment.production.ts for production builds
// (see fileReplacements in angular.json).
export const environment = {
  production: false,
  apiBaseUrl: 'http://127.0.0.1:8000/api',
};
